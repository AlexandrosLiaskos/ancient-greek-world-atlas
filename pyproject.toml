[project]
name = "ancient-greek-world-data"
version = "1.0.0"
description = "Scholarly data foundation for the Ancient Greek World Atlas"
requires-python = ">=3.11"
dependencies = [
  "Pillow>=10.0,<13",
]
license = { text = "MIT" }

[tool.unittest]
start-directory = "tests"
pattern = "test_*.py"
